/**
 * Configuración de producción (la que usa `npm run build`).
 *
 * Cambia `apiUrl` por la URL pública de tu backend antes de desplegar.
 * Debe coincidir con lo que el backend acepte en CORS_ORIGINS.
 */
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND.example.com'
};
